extends layouts/main-layout.pug

block content
	+createPostForm(userLoggedIn)
	.postsContainer

	+createPostModals(userLoggedIn)
	+createImageAttachModal()

block contentRight
	.rightSection.fixed-top
		h1 What you likes
		.likedPostsContainer

block scripts 
	script(src="/js/home.js") 